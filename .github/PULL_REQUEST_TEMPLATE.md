## Summary

<!-- What does this PR change and why? -->

## Test plan

- [ ] New tests added (or existing tests updated) covering the change
- [ ] `npm test` passes locally
- [ ] `npm run lint` passes locally
- [ ] `npm run build` succeeds
